# Reference edge profile.  Keep transport/session policy in the embedding
# service; bot tokens must never be committed to this file.
bind = 127.0.0.1:8080
workers = 4
max_json_bytes = 65536
max_updates_per_bot = 1024
mtproto_transport = external
test_dc = false
